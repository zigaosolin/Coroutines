using System;
using System.Collections.Generic;
using System.Text;

namespace Reactors.Tests
{
    class ReactorRecordingTest
    {
        // Reactor recording only works from "start". We do not
        // support snapshots

        // We might support snapshots when the reactor is cleared from
        // all the running coroutines
    }
}
